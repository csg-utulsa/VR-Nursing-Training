using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//Controller script for the in-world dialog system
//Creates or awakens four buttons, then listens for a callback from one of them with an answer. If it matches, do **something.** If it doesn't match, do **another something.**

//Creates four instances of InteractableDialogBtn





public class InteractableDialogController : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
